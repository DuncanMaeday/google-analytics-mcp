[build]                                                                                                                                                                                                   
  builder = "nixpacks"

  [deploy]
  startCommand = "pip install . && python -m ga4_mcp --transport streamable-http --port $PORT"
